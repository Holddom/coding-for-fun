	• 移位操作符 <<左移  >>右移
	• 位操作符 逻辑与 & 逻辑或| 异或^
	• ~求补运算 1变为0 0变为1
	• 位的操纵 指定的为清0 或设置为1
	• 或者检查某位是否为1
	• 赋值表达式的值就是左操作数的新值
	• EOF需要的位数比字符型值所能提供的位数要多
	• 这也是getchar返回一个整型值而不是字符值的原因
	• sizeof（）不对括号里的表达式求值，包括赋值操作
	• 自增自减操作符的结果不是被它们所修改的变量，而是变量值的拷贝
	• 所以++a=10; ++a的结果是a值的拷贝，不是变量本身，所以无法向一个值进行赋值
	
